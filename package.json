{
  "name": "cmdrx",
  "private": true,
  "version": "1.13.1",
  "description": "Extensible command console for Roblox developers. This fork was made to add small use cases that 'arent fit for production code'",
  "license": "MIT",
  "scripts": {
    "docs:dev": "moonwave dev --code CmdrX",
    "docs:build": "moonwave build --code CmdrX",
    "docs:publish": "moonwave build --code CmdrX --publish"
  },
  "devDependencies": {
    "moonwave": "^1.1.1"
  }
}
